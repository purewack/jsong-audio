// const {JSONg} = require('./src/JSONg')
// module.exports = {JSONg};

const {quanTime} = require('./src/quantime')
const {nextSection} = require('./src/nextSection')
const {buildSection} = require('./src/buildSection')
const {setNestedIndex, getNestedIndex} = require('./src/nestedIndex')
module.exports = {quanTime, nextSection, buildSection, setNestedIndex, getNestedIndex}